# متغيرات البيئة للتطبيق

# إعدادات الخادم
PORT=5000
NODE_ENV=development
JWT_SECRET=olive_store_secret_key_2024

# إعدادات قاعدة البيانات (للإنتاج على Vercel)
POSTGRES_URL=postgresql://username:password@hostname:port/database

# إعدادات البريد الإلكتروني
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
RECIPIENT_EMAIL=recipient@example.com

# ملاحظات:
# 1. للحصول على قاعدة بيانات PostgreSQL مجانية، اذهب إلى Vercel Dashboard > Storage > Create Database
# 2. لإعداد Gmail، استخدم App Password وليس كلمة المرور العادية
# 3. تأكد من إضافة هذه المتغيرات في Vercel Dashboard > Settings > Environment Variables